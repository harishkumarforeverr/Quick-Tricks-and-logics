 <Table
        getPopupContainer={(trigger) => trigger.parentNode}  // these is for not to render the content outside of the root div
        showSorterTooltip={false} // these one whether to show tooltip(popover) or no on the table sorting
        className="CustomTable" 
      />
