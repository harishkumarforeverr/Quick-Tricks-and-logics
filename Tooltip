.DashboardHealthRecord .ant-tooltip-inner {
  color: var(--half-white);
  background-color: var(--primary);
}


       <Tooltip
                    overlayClassName="DashboardHealthRecord"
                    title={item.issue}
                  >
                    <span>
                      {item?.issue?.slice(0, 10)}
                      {item?.issue?.length >= 11 && "..."}
                    </span>
                  </Tooltip>
